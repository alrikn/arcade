# todo list for the arcade project


### Core

- [ ] prompt the user for their name on initialisation (for the high score thing)
- [ ] add a function that uploads high scores to a file (needs to have the game name, player name and score) (only upload it if its a higher score than current one, otherwise makes no sense)
- [ ] add a function that retrieves high scores from a file (needs to have the game and returns the score) it won't care about the player name, i just wanna display high score.
- [ ] add a key map to restart the game
- [ ] add argument parsing to be able to specify graphical library.

### Menu
- [ ] display the high score (provided by the core) for each game in the menu
- [ ] add a "high scores" section in the menu that displays the high scores for each game.